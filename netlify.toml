# growtoo static site (Netlify)
# Public journal lives at /journal/ (and /journal/p/, /journal/g/).
# To serve the same pages on journal.growto.live later: add the subdomain in
# Netlify Domain management and redirect journal.growto.live/* → /journal/:splat
# (or set a domain alias with path prefix). No separate site binary required.
[build]
  publish = "."
  command = "echo 'Static site — no build step'"
  # Never auto-skip: Netlify had been marking every main push as Skipped.
  # exit 1 = "do not ignore" (continue the build).
  ignore = "exit 1"

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(self), microphone=(self), geolocation=(), payment=()"
    # Clickjacking + baseline; full script/connect CSP deferred (wallet inject + Firebase).
    Content-Security-Policy = "frame-ancestors 'none'; base-uri 'self'; object-src 'none'"

[[headers]]
  for = "/app/js/*"
  [headers.values]
    Cache-Control = "public, max-age=300, must-revalidate"

[[headers]]
  for = "/app/styles/*"
  [headers.values]
    Cache-Control = "public, max-age=300, must-revalidate"

[[headers]]
  for = "/app/index.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
