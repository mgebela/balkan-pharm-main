Firebase Auth email paste — project balpha-9dab9
================================================

Console:
https://console.firebase.google.com/project/balpha-9dab9/authentication/emails

Sender display name is already set via API to: growtoo
(From address stays noreply@balpha-9dab9.firebaseapp.com until the custom
domain below is verified and applied.)

------------------------------------------------------------
0) Google sign-in still says “balpha-9dab9” in mail / popup
------------------------------------------------------------
The site code cannot rename this. Project ID stays balpha-9dab9 forever.

A) Gmail “new sign-in” / Google popup (“Continue to balpha-9dab9…”)
   This is Google OAuth branding, not Firebase templates.

   1. Open:
      https://console.cloud.google.com/auth/branding?project=balpha-9dab9
      (older UI: APIs & Services → OAuth consent screen)
   2. App name: growtoo
   3. Support email + privacy: https://growto.live/privacy/
      Terms: https://growto.live/terms/
   4. App domain: growto.live  ·  logo: images/growtoo-mark-brass.png
   5. Save / publish. Unverified apps often still show the project id or
      balpha-9dab9.firebaseapp.com until Google brand verification.

   Do not change firebaseConfig.authDomain to growto.live until
   https://growto.live/__/auth/handler is served (Netlify does not, Firebase
   Hosting would). Changing it now breaks Google sign-in.

B) Firebase verify / reset mail still From: …@balpha-9dab9.firebaseapp.com
   Custom domain growto.live was started 2026-08-18 13:29 UTC and is still
   IN_PROGRESS (checked 2026-08-19). TXT + SPF are live; DKIM is not.

   Live now:
     TXT  growto.live  firebase=balpha-9dab9
     TXT  growto.live  v=spf1 include:_spf.firebasemail.com ~all

   Add these in Cloudflare DNS (DNS only / grey cloud — never proxied):

     Type   Name                      Target
     CNAME  firebase1._domainkey      mail-growto-live.dkim1._domainkey.firebasemail.com
     CNAME  firebase2._domainkey      mail-growto-live.dkim2._domainkey.firebasemail.com

   If Console shows different hosts, use the Console table, not this guess.

   Then:
   1. https://console.firebase.google.com/project/balpha-9dab9/authentication/emails
   2. Wait for green “Verification complete”
   3. Click Apply custom domain (waiting alone does not switch From)
   4. After apply, From becomes growtoo <noreply@growto.live> and action
      links leave balpha-9dab9.firebaseapp.com

C) %APP_NAME% in default subjects (“… for %APP_NAME%”)
   Public-facing name is already GROWTOO (Firebase project displayName).
   The live templates still use “Verify your email for %APP_NAME%” until you
   paste the subjects in sections 1–3 in the Console (API cannot set them).

Subjects + HTML bodies must be pasted in the Console
(API returns EMAIL_TEMPLATE_UPDATE_NOT_ALLOWED for subject;
body PATCH is ignored for this project — Console only).

------------------------------------------------------------
1) Email address verification
------------------------------------------------------------
Subject:
Verify your email · growtoo

Body file (copy all):
emails/firebase/verify-email.body.html

Keep %LINK% and %EMAIL% unchanged. Save.

------------------------------------------------------------
2) Password reset
------------------------------------------------------------
Subject:
Reset your password · growtoo

Body file (copy all):
emails/firebase/reset-password.body.html

Keep %LINK% and %EMAIL% unchanged. Save.

------------------------------------------------------------
3) Email address change
------------------------------------------------------------
Subject:
Confirm your new email · growtoo

Body file (copy all):
emails/firebase/change-email.body.html

Keep %LINK% and %NEW_EMAIL% unchanged. Save.

------------------------------------------------------------
Smoke check
------------------------------------------------------------
1. Sign up a fresh address on https://growto.live/dnevnik/
2. Open the verification email — expect forest card, brass CTA, growtoo seal mark
3. Optional: trigger password reset from Console Users → … or your reset UI
4. Confirm the action link still works (%LINK% was not edited out)

Do not paste transactional stubs (welcome / stage-sealed / market-settled).
